import React from 'react';
import {Box} from "@mui/material";

const ProductPage = () => {
    return (
        <Box component="section">Check you email, for confirm email.</Box>
    );
};

export default ProductPage;